# Build
build/
cmake-build-*/
*.cmake
CMakeCache.txt
CMakeFiles/
Makefile

# Binaries (built executables, no extension on macOS/Linux)
game_server
debug_test
simple_client
game_client
test_packet_buffer
test_network_stability
test_threading_system
test_ecs_system
simple_threading_test
advanced_threading_test
integration_test
benchmark_performance
*.dSYM/

# macOS
.DS_Store
.AppleDouble

# IDE
.vscode/
.idea/
*.xcodeproj/
*.suo
*.user

# Private docs — local only, not for public repo
docs/private/

# Third-party embedded git repos — git이 nested repo로 인식하지 않도록
third_party/asio/.git
third_party/spdlog/.git

# Portfolio / private materials
portfolio_code/
*.zip

# 환경변수 (비밀번호 포함) — 절대 커밋하지 말 것
.env
